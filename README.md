# Netflix-Queue-Project
A repository for a Netflix Queue Project.
# To run:
Navigate to the file using "cd" in the terminal. Then type "g++ netflix_queue.cpp".\
Then type "./a.out" to run the program. The program will give a prompt until you enter an invalid character/symbol or "e".
